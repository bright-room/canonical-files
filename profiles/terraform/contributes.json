{
  "_comment": ".terraform.lock.hcl は全リポでコミット必須(CI が terraform init -lockfile=readonly 前提)のため ignore してはいけない。*.tfvars と override.tf 系は全5リポでコミット実績ゼロを確認済み。mise.toml は create-only(初回のみ配布。以後の更新は各リポの Renovate に委ねる)。初期値の terraform はこのリポの Renovate が追従する。",
  ".gitignore": {
    "sections": [
      { "comment": "terraform", "ignores": [".terraform/", "*.tfstate", "*.tfstate.*", "*.tfvars", "*.tfvars.json", "crash.log", "crash.*.log", "override.tf", "override.tf.json", "*_override.tf", "*_override.tf.json", ".terraformrc", "terraform.rc"] }
    ]
  },
  "renovate.json": { "extends": ["github>bright-room/renovate-config:terraform"] },
  "mise.toml": { "template": "mise.toml.liquid", "tools": { "terraform": "1.15.7" } }
}
